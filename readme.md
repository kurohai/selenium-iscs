# Selenium Demo for ISCS


## Installation

Selenium driver for Python can be installed with pip:

`pip install -r requirements.txt`

## Usage

From a command line:

`python ./fb_login.py "username@example.com" "password"`
